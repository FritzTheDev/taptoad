// AppHeader.js

// Package Imports
import React from 'react';
import { IonHeader, IonToolbar, IonTitle } from '@ionic/react';

export const AppHeader = () => (
  <IonHeader>
    <IonToolbar color="primary">
      <IonTitle>Add.App Marketplace</IonTitle>
    </IonToolbar>
  </IonHeader>
);
